<Window x:Class="RealESRGAN_GUI.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="400"
        Height="130"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        FontFamily="Microsoft YaHei UI, DengXian, Segoe UI"
        UseLayoutRounding="True"
        TextOptions.TextRenderingMode="ClearType">

    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12">
        <Grid Margin="28,22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题行 -->
            <Grid Grid.Row="0">
                <TextBlock Text="Real-ESRGAN"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundBrush}"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="VersionText"
                           Text="v1.0"
                           FontSize="11"
                           Foreground="{DynamicResource SubtleForegroundBrush}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"/>
            </Grid>

            <!-- 副标题 + 状态 -->
            <Grid Grid.Row="1" Margin="0,6,0,0">
                <TextBlock Text="图像超分辨率工具"
                           FontSize="12"
                           Foreground="{DynamicResource MutedForegroundBrush}"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusText"
                           Text="正在启动..."
                           FontSize="11"
                           Foreground="{DynamicResource MutedForegroundBrush}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"/>
            </Grid>

            <!-- 极简进度条 -->
            <Grid Grid.Row="2" Height="2" VerticalAlignment="Bottom" ClipToBounds="True">
                <Border Background="{DynamicResource ProgressTrackBrush}" CornerRadius="1"/>
                <Border x:Name="Pulse"
                        Width="40"
                        Background="{DynamicResource AccentBrush}"
                        CornerRadius="1"
                        HorizontalAlignment="Left">
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="PulseTransform" X="-40"/>
                    </Border.RenderTransform>
                </Border>
            </Grid>
        </Grid>
    </Border>

    <Window.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard>
                <Storyboard RepeatBehavior="Forever">
                    <DoubleAnimation Storyboard.TargetName="PulseTransform"
                                     Storyboard.TargetProperty="X"
                                     From="-40"
                                     To="344"
                                     Duration="0:0:0.8"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
